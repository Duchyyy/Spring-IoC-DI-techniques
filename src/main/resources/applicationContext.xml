<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- enable scan component  -->
    <context:component-scan base-package="dev.andrejducho.spring"/>

    <!--
      with @Component we register bean
      and @Autowired in constructor, setter, method connect to
       probably first implementation of interface, so if we have multiple implementation
       use @Autowired field with qualifier with implementation string @Qualifier("summerTiresService")
      -->

</beans>